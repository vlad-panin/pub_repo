service:
  type: ClusterIP
  ports:
    http: 8083

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPU: "100%"
  targetMemory: "100%"